// Compatibility entry point. The maintained real-native suite now covers both
// the tools-only/multi-array cases and all four original acceptance workflows:
// zero-hole stock, R2 + margins + miter/edit, clipped end candidates, and library
// branch/local DXF side + end machining. Run either entry, not both.
import "./TubeDesignerPunchToolsOnlyNative.browser.mjs";
