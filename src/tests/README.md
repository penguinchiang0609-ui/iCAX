# tests

`tests` 放置测试代码和测试资料。

## 目录结构

- `icax-engine/`：对应 `src/icax-engine` 的后台平台测试。
- `iCAX-UI/`：对应 `src/iCAX-UI` 的 H5 前端框架测试。
- `icax-protocol/`：对应 `src/icax-protocol` 的协议测试。
- `legacy-readme.md`：旧测试目录说明的迁移保留文件。

每个被测项目在 `tests` 下保持一一对应目录，并在末级目录放置自己的测试工程。
