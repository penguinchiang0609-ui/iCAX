# foundation Tests

`foundation` 测试目录与 `src/icax/foundation` 一一对应，放置 foundation 下各项目的单元测试。

## 目录结构

- `Data/`：对应 `src/icax/foundation/Data` 的 Data 单元测试。
- `Task/`：对应 `src/icax/foundation/Task` 的 Task 单元测试。
