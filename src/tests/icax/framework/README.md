# framework Tests

`framework` 测试目录与 `src/icax/framework` 一一对应，放置 framework 下各项目的单元测试。

## 目录结构

- `ApplicationContext/`：对应 `src/icax/framework/ApplicationContext` 的 ApplicationContext 单元测试。
- `CommandHandler/`：对应 `src/icax/framework/CommandHandler` 的 CommandHandler 单元测试。
- `Database/`：对应 `src/icax/framework/Database` 的 Database 单元测试。
- `Mailbox/`：对应 `src/icax/framework/Mailbox` 的 Mail 通信单元测试。
- `PDO/`：对应 `src/icax/framework/PDO` 的 PDO 单元测试。
