# ApplicationContext Tests

`ApplicationContext` 测试目录与 `src/icax/framework/ApplicationContext` 一一对应，放置 ApplicationContext 项目的单元测试。

## 目录结构

- `ApplicationContextTest/`：基于 GoogleTest 的 ApplicationContext 单元测试工程。
