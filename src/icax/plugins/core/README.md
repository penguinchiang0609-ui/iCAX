# core

`core` 是 icax 平台自带的基础扩展集合，提供多数产品都会需要的通用组件、行为和服务。

## 目录结构

- `CoreComponent/`：基础组件，例如 Transform、Hierarchy、Tag、ProjectSetting。
- `CoreBehaviour/`：基础行为，例如 TransformBehaviour、HierarchyBehaviour。
- `CoreService/`：基础服务，例如资源、设置、日志等服务。
