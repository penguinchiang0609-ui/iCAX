# ProcedureCall

`ProcedureCall` 是 foundation 中的过程调用封装，提供类似 IPC 的 PCID、过程函数注册、查找和自动注册能力。

Mailbox 可以使用 `ProcedureCall` 的 `PCID` 作为消息类型码，但 `ProcedureCall` 本身不属于 Mailbox。

## 目录结构

当前目录直接放置 `ProcedureCall.vcxproj` 及其源码文件。
