# icax-workbench

`icax-workbench` 是 H5 前端通用工作台壳。它不表示某个具体业务产品，而是承载产品页面、布局、路由、通用 UI、Mailbox 和 PDO 前端封装。

## 目录结构

- `shell/`：前端应用壳和启动入口。
- `router/`：前端路由和产品页面注册。
- `layout/`：主布局、停靠区、菜单和面板容器。
- `common-ui/`：跨产品复用的基础 UI 组件。
- `mailbox/`：前端侧 Mailbox 封装。
- `pdo/`：前端侧 PDO 订阅、缓存和数据绑定。
