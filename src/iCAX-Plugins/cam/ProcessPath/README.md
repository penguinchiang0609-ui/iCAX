# ProcessPath

通用执行刀路插件，表达作业区中加工头实际经过的路径。

同一ProcessPath Entity随规划过程逐步增加：

- 空间曲线。
- 姿态场。
- 切割、穿孔、延时和参数切换等过程事件。
- 具体机床的轴轨迹。

插件依赖通用几何表达层和IntentToolpath，但不负责意图编辑、作业排序、运动学算法或具体后处理器。后处理器直接读取ProcessPath并导出控制器文件；导出文件不是项目中的Path或持久领域对象。现有Laser3DCAM中的`CPathComponent`保持原状，后续由产品按功能替换，不迁移进本插件。
