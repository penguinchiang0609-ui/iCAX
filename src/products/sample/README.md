# sample

`sample` 是示例产品，用于展示一个具体产品如何同时拥有 backend 扩展、frontend 页面和产品级协议。

## 目录结构

- `backend/`：示例产品的 C++ 组件、行为和服务。
- `frontend/`：示例产品的 H5 页面、面板和组件。
- `protocol/`：示例产品自己的 Mailbox/PDO 协议。
- `manifest.json`：示例产品装配清单。
