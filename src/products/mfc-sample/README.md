# mfc-sample

`mfc-sample` 是当前 MFC 示例产品，用于保留和验证传统桌面宿主下的 icax 接入方式。

## 目录结构

- `backend/`：MFC 示例 C++ 工程。
- `frontend/`：MFC 示例的 H5 页面位置，当前为空。
- `protocol/`：MFC 示例产品级协议，当前为空。
- `manifest.json`：MFC 示例产品装配清单。
