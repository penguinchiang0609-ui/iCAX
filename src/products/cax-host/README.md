# cax-host

`cax-host` 是当前 CAXHost 宿主产品。它负责承载 icax backend 和 H5 frontend 的同进程运行形态。

## 目录结构

- `backend/`：CAXHost C++ 宿主工程。
- `frontend/`：CAXHost 产品页面，当前为空。
- `protocol/`：CAXHost 产品级协议，当前为空。
- `manifest.json`：CAXHost 产品装配清单。
