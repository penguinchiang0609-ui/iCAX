# 项目协作约定

## 参数联动审查范围

- 参数联动完善必须覆盖产品、管型、模具三块全部现有模板；用户举槽口例子不代表只处理槽口。
- 显示/禁用条件复用 parameterConditions.mjs，资源库、产品内嵌编辑和参数示意图保持一致，不另写条件解释器。
- 模板声明条件必须与生成脚本的实际适用分支一致；关闭上级选项保留草稿值，但不让不适用的子选项继续影响几何或阻断生成。
- 产品条件中的枚举比较值也必须属于该模板实际 choices，不能把通用模板的 none/horizontal/manual_count 条件直接加到选项已收窄的固定款。修改后须验证原生 GenerateProductTemplatePreview 接口；只跑 Python 生成和前端条件判断不能代替原生描述符校验。
- 模板内部可派生计算参数，但返回的 parameters 必须与宿主规范化输入完全一致，不能夹带内部字段或重命名后的键。包装/合并模板必须回归输入不变、返回参数一致，并用受影响模板实际调用原生预览接口；不相关模板的预览通过不能代替验证。

## 产品清单部署

- 不得将源码的 product.manifest.json 原样复制到运行目录。源码清单的 DLL 路径含 Platform/Configuration，运行目录再次展开会重复 x64/Debug。
- TubeDesigner 清单使用 src/tools/build/deploy_tube_designer_manifest.ps1 -OutputDirectory <二进制目录> 部署；部署前检查全部模块路径，保留源码清单用于源码目录启动。

## V 槽圆角的产品语义

- V 槽圆角与释放孔同属槽根避空：底部局部多切除材料，减少折弯挤压与干涉。
- 保留用户认可的原圆角几何；不得将其替换为相切过渡、折弯成形半径或弧长展开模型。
- 已撤除误加的“相切圆角展开槽”（developed）及独立右圆角参数，不得重新引入作为原圆角的改进。
- K 补偿与槽根避空是不同概念，不得用展开公式重新定义圆角。

## 管型反解必须直接求解

- fitting.py 根据完整输入轮廓的几何关系直接求参数、旋转角和平移；不匹配返回 False。
- 反解运行链路禁止调用 build、evaluate_section、_evaluate；禁止数值搜索正向参数、生成候选轮廓或重建比较。
- 正向生成只允许在测试准备样本时使用。测试必须禁用正向入口后执行反解，包括批量模板目录读取。
- 共享层只能提供几何解析、关系查询与计算，管型规则属于各模板。
- 未实现的直接反解必须报告 unsupported，不能用默认参数、输入轮廓直存或通用迭代冒充支持。

## 前端刷新不得破坏正在进行的操作

用户多次明确要求：修改参数、重新生成模型或异步响应导致刷新时，左、右侧栏必须保留当前焦点、文本光标／选区，以及各自和嵌套容器的滚动位置。

- 优先局部更新模型和必要 DOM，不重建整个场景或侧栏。
- 模具／管型页常规编辑和预览回调使用 libraryDomPatch 局部更新路径；不得重新退回 mount.innerHTML 重建。回归测试还必须验证画布、输入框节点及事件监听器没有被替换。
- 必须替换 DOM 时，复用公共交互状态保护机制；在实际替换前捕获最新状态，不能使用请求发出时的旧位置。
- 恢复焦点使用 preventScroll，再恢复滚动位置；字段被移除时不能把焦点误给其他字段。
- 不跨页面或跨项目抢回焦点，不用延迟回调覆盖用户后续滚动和输入。
- 涉及刷新流程的变更必须增加真实浏览器回归：左右侧栏同时滚动、输入焦点和选区、异步响应前继续滚动、条件字段变化。
- 不得把“节点能显示”或纯字符串测试视为上述交互已验证。
