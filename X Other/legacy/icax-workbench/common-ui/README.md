# common-ui

`common-ui` 放置跨应用复用的前端基础 UI 组件。

基础 UI 只提供通用控件，不承载产品业务逻辑。产品 UI 应通过组合这些控件构建自己的页面和面板。

## 目录结构

- `README.md`：本目录说明。
- 后续可放置按钮、图标按钮、菜单、标签页、树、表格、属性面板、弹窗、通知、空状态和加载状态。

## 设计要求

- 工具按钮优先使用图标。
- 数据密集区域使用表格、树、属性面板和分栏布局。
- 控件尺寸稳定，避免状态变化导致布局跳动。
