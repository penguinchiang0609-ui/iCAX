# ResourcePool

本目录记录 iCAX Engine Foundation 层资源池的规格与方案文档。

对应源码目录 `src/icax/foundation/ResourcePool/`。它提供进程内 `ResourceType + ResourceID` 到资源对象的注册、查找、替换、删除、枚举和轻量元信息维护能力。

## 目录结构

- `ResourcePool规格文档.md`：资源池基础能力的外部规格。
- `ResourcePool方案文档.md`：资源池基础能力的实现方案。
