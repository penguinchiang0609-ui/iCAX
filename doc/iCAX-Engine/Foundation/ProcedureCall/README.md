# ProcedureCall

`ProcedureCall` 是 iCAX Engine Foundation 层的过程调用封装，对应源码目录 `src/icax/foundation/ProcedureCall/`。

它提供类似 IPC 的 PCID、过程函数注册、查找和自动注册能力。Mail 通信可以使用它做消息类型路由，但它不属于 Mail 通信。

## 目录结构

- `ProcedureCall规格文档.md`：ProcedureCall 的外部规格。
- `ProcedureCall方案文档.md`：ProcedureCall 的实现方案。
